// config.js
// Quando o jogo é servido pelo próprio servidor (Railway), não precisa mudar nada:
// o navegador já está na URL certa. Deixe vazio para usar a mesma origem.
window.RIORISE_CONFIG = {
  serverUrl: "" // ex: "https://rio-rise-rp.up.railway.app" se for usar de outro lugar
};
